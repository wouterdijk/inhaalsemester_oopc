#include "auto.hpp"

void car::print(){
    a.draw( w );
    b.draw( w );
    c.draw( w );
    d.draw( w );
    e.draw( w );
    f.draw( w );
    r.print();
    c1.draw( w );
    c2.draw( w );

    w.flush();
}